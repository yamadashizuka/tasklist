<%= form_for(@statuscode) do |f| %>
  <% if @statuscode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statuscode.errors.count, "error") %> prohibited this statuscode from being saved:</h2>

      <ul>
      <% @statuscode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
